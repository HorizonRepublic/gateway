---
sidebar_position: 2
sidebar_label: "Installation"
title: "Installation — Server, SDK, and a local NATS"
description: "Install the gateway-server binary, add the gateway-sdk package to your NestJS project, and run a JetStream-enabled NATS locally."
schema:
  type: Article
  headline: "Installation — Server, SDK, and a local NATS"
  description: "Install the gateway-server binary, add the gateway-sdk package to your NestJS project, and run a JetStream-enabled NATS locally."
  datePublished: "2026-04-27"
  dateModified: "2026-04-27"
---

# Installation

:::info Coming soon
Concrete install steps land with the SDK package publish and the gateway-server release. Expected content: SDK install (npm/pnpm/yarn tabs), Go server install (binary download or `go install`), local NATS via Docker, peer dependencies, runtime requirements (Node.js, Go, NATS Server versions).
:::

## Where to next

- [Quick Start](/docs/getting-started/quick-start) — wire the SDK and server together for the first time.

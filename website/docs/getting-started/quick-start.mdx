---
sidebar_position: 3
sidebar_label: "Quick Start"
title: "Quick Start — From zero to a routed HTTP request"
description: "A working example: declare a route in NestJS, run the gateway-server, send an HTTP request, see it round-trip through NATS."
schema:
  type: Article
  headline: "Quick Start — From zero to a routed HTTP request"
  description: "A working example: declare a route in NestJS, run the gateway-server, send an HTTP request, see it round-trip through NATS."
  datePublished: "2026-04-27"
  dateModified: "2026-04-27"
---

# Quick Start

:::info Coming soon
The end-to-end walkthrough — declare a route via SDK decorator, register it with the registry, start the gateway-server, send the first request, observe it in the logs — lands with the SDK and server ports.
:::

## Where to next

- [Declaring routes](/docs/sdk/declaring-routes) — the decorators and the registry contract.
- [Server configuration](/docs/server/configuration) — env vars, defaults, tuning knobs.
- [Architecture overview](/docs/architecture/overview) — what just happened, in pictures.
